function [ features ] = generateFeatures(raw, resolution)
r_keys = keys(raw);
for k_i=1:size(raw)
    key = r_keys(k_i)
    fdbs = features(key);
    rdbs = raw(key);
    for i=1:len
    extractedFeatures = featureExtractor(raw, RESOLUTION);
    fdbs(length(fdbs)+1) = {extractedFeatures};
end

end

